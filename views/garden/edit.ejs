<h2>Update <%=currentUser.first_name %>'s Garden</h2>

<div class="garden">
  <form class="edit-garden" name="gardenUpdate" action="/garden/update/<%= garden.id %>" method="post">
    <input type="hidden" class="form-control" name="user_id" value="<%= currentUser.id %>">
    <div class="form-group">
      <label for="gardenName">Garden Name</label>
      <input id="gardenName" type="text" name="name" value="<%= garden.name %>">
    </div>

    <div class="form-group">
      <label for="gardenDescription">Garden Description</label>
      <input id="gardenDescription" type="text" name="description" value="<%= garden.description %>">
    </div>

    <div class="form-group">
      <label for="gardenLocation">Location</label>
      <input id="gardenLocation" type="text" name="location" value="<%= garden.location %>">
    </div>

    <a class="btn btn-info" role="button" href="/garden/show/<%= currentUser.id %>">Back to Gardens</a>
    <input class="btn btn-primary" type="submit">
  </form>
</div>

<div class="gardenInfo">
  <p>What can we put there to help with garden planning?</p>


</div>
